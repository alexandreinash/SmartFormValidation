# Copy this file to .env and fill in your values

# Server Configuration
PORT=5000

# Database Configuration (Local MySQL - Development)
# UPDATE THESE VALUES TO MATCH YOUR LOCAL MYSQL SETTINGS!
DB_HOST=localhost
DB_PORT=3306
DB_NAME=db_smartform
DB_USER=root
DB_PASS=123123
DB_SSL=false

# Database Configuration (Google Cloud SQL - Production)
# Uncomment and use these for Google Cloud SQL instead:
# DB_HOST=YOUR_CLOUD_SQL_PUBLIC_IP
# DB_PORT=3306
# DB_NAME=smart_form_validator
# DB_USER=root
# DB_PASS=your_cloud_sql_password
# DB_SSL=true

# Authentication
JWT_SECRET=dev_secret_change_in_production_please_use_random_string

# Google Cloud Natural Language API
# Set to false if you don't have Google Cloud credentials yet
GCLOUD_NLP_ENABLED=false
GOOGLE_APPLICATION_CREDENTIALS=C:/path/to/your/service-account.json

# Google Cloud Vision API (for image/ID recognition)
GCLOUD_VISION_ENABLED=false
# Uses same service account as NLP: GOOGLE_APPLICATION_CREDENTIALS

# Optional: Enable SQL query logging
# DB_LOGGING=true

# Rate Limiting (optional)
# RATE_LIMIT_WINDOW_MS=900000
# RATE_LIMIT_MAX_REQUESTS=100
# RATE_LIMIT_SUBMISSION_MAX=1000  # Max form submissions per window (default: 1000 in dev, 10 in production)
# RATE_LIMIT_SUBMISSION_WINDOW_MS=900000  # Window in milliseconds (default: 15 minutes)
# RATE_LIMIT_AUTH_MAX=50  # Max auth attempts per window (default: 50 in dev, 5 in production)

# Email Notifications
# Set EMAIL_ENABLED=true to enable email notifications
EMAIL_ENABLED=false
EMAIL_FROM=noreply@smartformvalidator.com

# Email Provider Configuration
# Options: smtp (default), sendgrid, ses
EMAIL_PROVIDER=smtp

# SMTP Configuration (for development/testing)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=your-email@gmail.com
SMTP_PASS=your-app-password

# SendGrid Configuration (alternative)
# SENDGRID_API_KEY=your_sendgrid_api_key

# AWS SES Configuration (alternative)
# AWS_REGION=us-east-1
# AWS_ACCESS_KEY_ID=your_access_key
# AWS_SECRET_ACCESS_KEY=your_secret_key

# WebSocket / Real-time Updates
# Frontend URL for CORS (used by Socket.IO)
FRONTEND_URL=http://localhost:5173

